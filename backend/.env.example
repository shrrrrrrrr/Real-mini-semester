# 知源后端环境配置示例：复制为 .env 并填写
# LLM（OpenAI 兼容协议；示例为 DeepSeek，可换 GLM/Qwen/OpenAI）
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=
LLM_MODEL=deepseek-chat

# 本地嵌入模型（首次运行自动下载，之后离线可用）
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# 服务
HOST=127.0.0.1
PORT=8000
