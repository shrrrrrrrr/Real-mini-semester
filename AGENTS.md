# 项目全局协作规范

本文件规定 Real mini semester 项目中所有 Agent 必须遵守的长期要求。执行任何任务前先阅读本文件；用户在当前对话中提出的新要求优先于本文件。

## 项目定位与分工

- 项目服务于软件工程学院小学期的独立全栈作品要求，暂定为面向大学生课程学习的 Web 应用。
- 用户负责筛选参考项目中的功能与设计、确认产品取舍；Agent 负责将确认后的方案落地为可运行、可维护、可验证的产品。
- 未经用户确认，不将项目扩展为大型功能集合；产品名称、功能范围、视觉方向和版本计划以用户最终确认的内容为准。

## 代码与架构

1. 使用适合项目规模、便于维护的架构。前端、后端、数据访问和 AI 服务之间的职责与边界必须清晰。
2. 代码必须保留清晰、足量的中文注释，重点解释：业务规则、数据流、AI 输出约束、边界情况以及不直观的工程决策。
3. 不为显而易见的语法或变量赋值逐行添加冗余注释；注释应帮助后续维护者理解“为什么这样做”。
4. Web 版本稳定且移动端体验适配后，评估并在可行时交付 Android 与 iOS 应用，优先采用可复用 Web 代码的方案。

## 开源参考与素材

1. 可以下载和研究相关开源仓库，参考其功能、设计或局部实现。
2. 不得将参考仓库整体、无关文件或未经适配的代码直接提交到本仓库。
3. 引入的代码、资源或思路必须与本项目具体需求有关，并补充适配实现与必要的中文注释。
4. 使用前核对许可证、署名和再分发要求，并在项目文档中保留必要说明。

## Git 与交接

1. 全程使用 Git 管理；每项完整工作使用语义明确的提交信息。
2. 完成工作并验证后，推送到用户仓库：`https://github.com/shrrrrrrrr/Real-mini-semester`。
3. 每次完成可交付工作后，必须更新根目录 `HANDOFF.md`，至少记录：已完成内容、验证结果、当前状态、下一步工作、风险和待用户操作。
4. `HANDOFF.md` 不重复本文件的长期规则；其中应提示后续 Agent 先阅读本文件。

## 部署、数据与外部授权

1. Web 前端部署优先使用 Vercel。
2. 需要云端数据库、用户鉴权、文件存储或服务端能力时优先使用 Supabase。
3. 涉及账户登录、API 密钥、付费、第三方平台授权、部署环境变量或其他必须由用户确认的外部操作时，不得假设已获授权。
4. 需要用户操作时，给出清晰、按顺序、可逐步执行的说明，并说明每一步的目的和预期结果。

## 版本管理

1. 若项目采用分版本开发，必须先与用户协商并确认版本计划，再开始对应版本的实现。
2. 版本按明确的可交付能力划分；不要为了形式创建没有实际价值的小版本。
3. 已确认的版本范围、变更原因和完成状态必须同步记录在 `HANDOFF.md`。
