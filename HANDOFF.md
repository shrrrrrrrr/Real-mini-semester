# Real mini semester — 项目交接文档

> 本文档是本项目的持续交接记录。每次完成一项可交付工作后，执行该工作的 Agent 必须更新“当前状态”“变更记录”“下一步工作”和“待用户操作”，使新 Agent 可以无需重新梳理上下文即可继续。

## 1. 项目目标

待用户完成产品功能筛选后，开发一个面向大学生课程学习的全栈 Web 应用。项目暂定方向为“课程资料智能问答与学习规划助手”：围绕课程资料、可信问答、测验错题和学习计划形成闭环。

产品名称、最终功能范围、视觉方向和版本计划须在用户确认后补充；不得自行扩展为未确认的大型功能集合。

## 2. 长期开发规范（用户总指令）

1. 代码使用清晰、足量的中文注释，重点解释业务规则、数据流、AI 输出约束和不直观的工程决策；不为显而易见的语法逐行添加冗余注释。
2. 使用适合项目规模、便于维护的架构开发 Web 应用。前端、后端、数据访问和 AI 服务的边界应清晰。
3. 在 Web 版本稳定且功能适配移动端后，评估并在可行时制作 Android 和 iOS 应用；优先采用能复用 Web 代码的方案。
4. 全程使用 Git 管理。每项完整工作应有语义明确的提交；完成后推送至用户仓库：`https://github.com/shrrrrrrrr/Real-mini-semester`。
5. 可以下载和研究相关开源仓库，但不得将参考仓库整体提交到本项目。仅引入与本项目需求相关的代码、资源或思路，并补充适配本项目的实现和中文注释；遵守其许可证与署名要求。
6. 每次工作完成后必须更新本文件，记录已完成内容、验证结果、下一步工作、风险与需要用户处理的事项。
7. 前端部署优先使用 Vercel；需要云端数据库、鉴权、文件存储或服务端能力时优先使用 Supabase。
8. 涉及用户账户登录、密钥、付费、第三方平台授权、部署环境变量或其他必须由用户确认的操作时，必须以可执行的逐步操作说明提出请求，不得假设已获授权。
9. 若采用分版本开发，先和用户协商确认版本计划，并将确认结果记录在本文档。版本应按可交付能力划分，不创建无实际价值的小版本。

## 3. 当前状态

- 日期：2026-09-02
- 本地工作区：已从用户指定仓库初始化。
- 远程仓库：初始为空。
- 已确定：项目服务于“软件工程学院小学期”的独立全栈作品要求；用户将先筛选参考项目的功能与设计。
- 尚未确定：正式产品名称、必做功能、UI 方向、技术栈细节、版本计划、是否需要移动端交付。

## 4. 参考项目（仅供调研）

- `THU-MAIC/OpenMAIC`：材料驱动的互动课程、测验和学习活动。
- `santanu2908/chat-with-pdf-rag`：带来源引用的 PDF 问答流程。
- `Apyhtml20/PaperBrain`：资料问答、摘要、测验、闪卡与学习记录。
- `Kashif-Mustari/Smart-Student-Success-Agent`：学习计划、测验与学习看板。
- `zeeshanparwez/VidyaAI`：学习计划、测验和薄弱点分析。
- `devesh-69/student-task-tracker`：作业任务与 AI 子任务拆解。
- `shreyashankar/planner`：基于截止日期与可用时间的任务排程。
- `vincentaayush/BoilerTrack`：课程、作业、待办和学习资源的界面组织。

## 5. 变更记录

### 2026-09-02 — 初始化交接规范

- 从用户指定的 GitHub 仓库初始化本地工作区；远程仓库为空。
- 新建本交接文档，记录用户的全部长期开发规范、当前状态和调研参考方向。

## 6. 下一步工作

等待用户给出参考项目筛选结果和希望保留的功能。收到后：

1. 整理功能范围与用户故事；
2. 与用户确认少量、有明确交付价值的版本计划；
3. 设计信息架构、数据模型、技术方案和 UI 方向；
4. 初始化项目并开始实现第一个已确认版本。

## 7. 待用户操作

- 无。

